version: "3"
services:
  visonic:
    networks:
      - proxy
    read_only: true
    deploy:
      replicas: 1
      labels:
        - com.df.notify=true
        - com.df.distribute=true
        - com.df.serviceDomain=visonic.p.cns.me
        - com.df.port=3000
        - com.df.servicePath=/
        - com.df.httpsOnly=true
        - com.df.xForwardedProto=true
      restart_policy:
        condition: any
        delay: 5s
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: pause
        monitor: 60s
        max_failure_ratio: 0.3
    image: pinked/visonic_control
    environment:
      - USER_CODE=${USER_CODE}
      - PANEL_WEB_NAME=${PANEL_WEB_NAME}
      - HOST=${HOST}
      - USER_ID=${USER_ID}
      - REFRESH=${REFRESH}
      - SECRET=${SECRET}

networks:
  proxy:
    external: true